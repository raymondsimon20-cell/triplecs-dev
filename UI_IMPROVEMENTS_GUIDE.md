# Removed

This document referenced components (`SimplifiedTradeWorkflow`, `SettingsPanel`-as-doc) that don't match the current codebase — it became actively misleading.

The 2026-05 dashboard redesign is the current direction. The new IA is implemented in `app/dashboard/page.tsx` (Today / Portfolio / History tabs).

Safe to `rm` this file.
