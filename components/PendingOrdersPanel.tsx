// Removed in the 2026-05 dashboard redesign. Was only imported by
// DailyReviewWizard, which itself was orphaned — transitively dead code. Kept
// as an empty stub (workspace no-delete constraint). Safe to `rm` this file.
export {};
