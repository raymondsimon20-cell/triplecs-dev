// Removed in the 2026-05 dashboard redesign. Logic is duplicated inside
// IncomeHub, which is what the dashboard actually renders. Kept as an empty
// stub instead of deleted (workspace no-delete constraint). Safe to `rm`.
export {};
