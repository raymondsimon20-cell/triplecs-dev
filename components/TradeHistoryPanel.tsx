// Removed in the 2026-05 dashboard redesign. TradeHub embeds its own order
// history — this standalone panel was orphaned. Kept as an empty stub instead
// of deleted (workspace no-delete constraint). Safe to `rm` this file.
export {};
