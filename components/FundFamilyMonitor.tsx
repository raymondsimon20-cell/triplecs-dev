// Removed in the 2026-05 dashboard redesign. Component was orphaned (zero
// imports across the codebase). Kept as an empty stub instead of deleted to
// satisfy the workspace's no-delete constraint. Safe to `rm` this file.
export {};
